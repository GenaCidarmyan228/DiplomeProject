<Page x:Class="KitchenManager.ClientsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="Клиенты" Loaded="Page_Loaded" Background="{StaticResource AppBackground}">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Шапка с заголовком, поиском и кнопками -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="База клиентов" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,0,20,0"/>

            <!-- Поле поиска -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Поиск:" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14" FontWeight="SemiBold"/>
                <TextBox x:Name="TxtSearch" Width="250" Height="35" TextChanged="TxtSearch_TextChanged"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="+ Новый клиент" Width="140" Margin="0,0,10,0" Click="BtnAdd_Click" Style="{StaticResource PrimaryBtnStyle}"/>
                <Button Content="Удалить" Width="100" Click="BtnDelete_Click" Style="{StaticResource DangerBtnStyle}"/>
            </StackPanel>
        </Grid>

        
        <Border Grid.Row="1" Background="{StaticResource CardBackground}" CornerRadius="10" Effect="{StaticResource CardShadow}">
            <DataGrid x:Name="DGridClients" AutoGenerateColumns="False" IsReadOnly="True" Margin="15" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ID_Client}" Width="60" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                    <DataGridTextColumn Header="ФАМИЛИЯ" Binding="{Binding Surname}" Width="*"/>
                    <DataGridTextColumn Header="ИМЯ" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="ОТЧЕСТВО" Binding="{Binding Patronymic}" Width="*"/>
                    <DataGridTextColumn Header="ТЕЛЕФОН" Binding="{Binding Phone}" Width="150"/>
                    <DataGridTextColumn Header="АДРЕС" Binding="{Binding Address}" Width="250"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>