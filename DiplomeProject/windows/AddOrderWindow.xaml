<Window x:Class="KitchenManager.Windows.AddOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:KitchenManager.Windows"
        Title="Новый заказ" Height="800" Width="650"
        Icon="/resources/unnamed.png" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource AppBackground}"
        Loaded="Window_Loaded">

    <Window.Resources>
        <local:StockColorConverter x:Key="StockColorConverter"/>
    </Window.Resources>

    <Border Background="{StaticResource CardBackground}" CornerRadius="10" Margin="20" Padding="30" Effect="{StaticResource CardShadow}">
        <StackPanel>
            <TextBlock Text="Оформление заказа" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Клиент:" FontSize="18" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="0,0,0,20"/>
                <ComboBox x:Name="ComboClients" Grid.Row="0" Grid.Column="1" SelectedValuePath="ID_Client" FontSize="18" Height="45" VerticalContentAlignment="Center" Margin="0,0,0,20" Padding="5">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="ID: " Foreground="#A0AEC0" FontSize="18"/>
                                <TextBlock Text="{Binding ID_Client}" FontWeight="Bold" Foreground="{Binding ClientIdColor}" FontSize="18"/>
                                <TextBlock Text=" — " Foreground="#A0AEC0" FontSize="18"/>
                                <TextBlock Text="{Binding Surname}" FontSize="18"/>
                                <TextBlock Text=" "/>
                                <TextBlock Text="{Binding Name}" FontSize="18"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Товары:" FontSize="18" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Top" Margin="0,10,0,20"/>
                <ListBox x:Name="ListEquipment" Grid.Row="1" Grid.Column="1" Height="150" Margin="0,0,0,20" ScrollViewer.VerticalScrollBarVisibility="Auto" SelectionMode="Multiple" BorderBrush="#CED4DA" BorderThickness="1">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox Content="{Binding Title}" IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Text="Кол-во:" Margin="15,0,5,0" Foreground="Gray" VerticalAlignment="Center"/>
                                    <TextBox Text="{Binding SelectedQuantity, UpdateSourceTrigger=PropertyChanged}" Width="35" TextAlignment="Center" FontWeight="Bold"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="25,5,0,0">
                                    <TextBlock Text="На складе: " Foreground="Gray" FontSize="12"/>
                                    <TextBlock Text="{Binding StockQuantity}" FontWeight="Bold" Foreground="{Binding StockQuantity, Converter={StaticResource StockColorConverter}}" FontSize="12"/>
                                    <TextBlock Text=" шт." Foreground="Gray" FontSize="12"/>
                                </StackPanel>
                                <ProgressBar Value="{Binding StockQuantity}" Maximum="20" Height="4" Margin="25,5,0,0" BorderThickness="0" Foreground="{Binding StockQuantity, Converter={StaticResource StockColorConverter}}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Услуги:" FontSize="18" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Top" Margin="0,10,0,20"/>
                <ListBox x:Name="ListServices" Grid.Row="2" Grid.Column="1" Height="110" Margin="0,0,0,20" ScrollViewer.VerticalScrollBarVisibility="Auto" SelectionMode="Multiple" BorderBrush="#CED4DA" BorderThickness="1">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Title}" IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" FontSize="16" Margin="5"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Сотрудники:" FontSize="18" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Top" Margin="0,10,0,30"/>
                <ListBox x:Name="ListEmployees" Grid.Row="3" Grid.Column="1" Height="110" Margin="0,0,0,30" ScrollViewer.VerticalScrollBarVisibility="Auto" SelectionMode="Multiple" BorderBrush="#CED4DA" BorderThickness="1">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" FontSize="16" Margin="5">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Surname}"/>
                                    <TextBlock Text=" "/>
                                    <TextBlock Text="{Binding Name}"/>
                                    <TextBlock Text=" (" Foreground="Gray" FontSize="14"/>
                                    <TextBlock Text="{Binding Positions.PositionName}" FontWeight="SemiBold" Foreground="Gray" FontSize="14"/>
                                    <TextBlock Text=")" Foreground="Gray" FontSize="14"/>
                                </StackPanel>
                            </CheckBox>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <Button x:Name="BtnAdd" Content="Оформить заказ" Click="BtnSave_Click" Height="55" FontSize="20" FontWeight="Bold" Style="{StaticResource SuccessBtnStyle}"/>
        </StackPanel>
    </Border>
</Window>