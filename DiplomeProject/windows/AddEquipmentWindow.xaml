<Window x:Class="KitchenManager.Windows.AddEquipmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Товар" Height="480" Width="360"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource AppBackground}">

    <Border Background="{StaticResource CardBackground}" CornerRadius="10" Margin="20" Padding="25" Effect="{StaticResource CardShadow}">
        <StackPanel>
            <TextBlock Text="Данные о товаре" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Название:" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Title}" Margin="0,0,0,12"/>

            <TextBlock Text="Описание:" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Description}" Margin="0,0,0,12"/>

            <TextBlock Text="Цена (руб):" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Price}" Margin="0,0,0,12"/>

            <TextBlock Text="Количество на складе (шт):" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding StockQuantity}" Margin="0,0,0,25"/>

            <Button Content="Сохранить" Click="BtnSave_Click" Style="{StaticResource SuccessBtnStyle}"/>
        </StackPanel>
    </Border>
</Window>